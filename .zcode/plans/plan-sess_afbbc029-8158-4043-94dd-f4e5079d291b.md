# Plan — Login multi-tenant fiel al branding (by-host / by-slug) con "by FRIG" sutil

## 1. Normalizador completo del payload público (`src/lib/api/branches.ts` → `normalizePublicLoginTheme`)
Mapear los campos que hoy se descartan:
- `algorithm` ← `algorithm` || `color_mode` (alias)
- `login_welcome_message` ← `login_welcome_message` || `welcome_message`
- `tagline` ← `login_subtitle` || `subtitle` || `tagline`
- `borderRadius` ← `ui_preferences.border_radius_px` (número > 0)
- `motion` ← `ui_preferences.motion_enabled` (boolean)

(`BranchThemeConfig` ya tiene todos estos campos — solo falta mapearlos.)

## 2. Login del tenant (`src/app/(auth)/login/page.tsx`)
- **Identidad Frig condicional**: extraer los overrides cobre a una constante compartida (p. ej. `FRIG_IDENTITY_STYLE` en `src/lib/frig-identity.ts`) y aplicarlos solo cuando `!brandTheme` (localhost / dominios Frig). Con tenant: cero overrides — `applyThemeConfig` ya deriva toda la paleta desde su primary (fix anterior) incluyendo `borderRadius`.
- **Clase `dark` y `text-white` condicionales** en la sección: solo para identidad Frig o tenant oscuro; tenants claros usan tokens `:root` y `text-foreground`.
- **Header con la info del tenant** (cuando `brandTheme`): `BrandLogo` (ya importado, soporta logo + fallback iniciales) + título `login_welcome_message || app_name` (font-display) + subtítulo `tagline` en muted. Sin tenant: se mantiene `FrigWordmarkMatrix`.
- **Footer condicional**: tenant → "by FRIG" sutil (muted, text-xs) con link a `https://github.com/FelipeBarraza6/frig` (nueva pestaña); Frig → texto actual.
- **`document.title`** = `{app_name} — Iniciar sesión` (o FRIG por defecto).

## 3. Mismo trato en `/forgot-password` y `/reset-password/[token]`
- Agregar el effect público `fetchPublicLoginThemeByHost()` + `applyThemeConfig` (crítico para el link de email de reset en dominio propio: carga directa, sin sesión).
- Overrides cobre → solo sin tenant; logo/nombre del tenant con `BrandLogo` (reemplaza el PNG de Frig); footer "by FRIG" → repo condicional; `document.title` correspondiente.

## 4. Fuera de alcance (no tocar)
Social links, website_url y motion wiring (el sistema ya deriva la paleta; el motion es preferencia de dialog). La caja de credenciales demo (`?branch=pos` con `LANDING_USE_CASES`) sigue igual.

## 5. Verificación
- `tsc --noEmit` + eslint + `next build`.
- Navegador: (a) `/login` plano → identidad Frig intacta (regresión); (b) `/login?branch=pos` → si el backend expone el theme demo, verificar header de marca + paleta naranja en inputs/botones; (c) simulación de cascada: setear vars de `:root` en rojo Mani vía evaluate y confirmar que inputs/botones/borders las respetan (valida que el fix de overrides condicionales sirve para by-host).
- Commit + push a `origin/master` (build verde primero).